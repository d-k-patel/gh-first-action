name: first workflow

on: workflow_dispatch

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Print greetings
              run: echo "hello user!"
            - name: Print goodbye
              run: echo "bye user!"
